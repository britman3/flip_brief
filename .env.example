# Anthropic API Key for Claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# EPC API Key (optional, get free key from https://epc.opendatacommunities.org/)
# Without this, EPC data won't be fetched but Land Registry data will still work
EPC_API_KEY=your-epc-api-key-here
